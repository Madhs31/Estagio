<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>

  <!-- Bulma CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">

  <style>
    .section {
      background-color: #2c2c2c;
      padding: 4rem 2rem;
      min-height: 100vh;
      display: flex;
    }

    .section-title {
      text-align: center;
      margin-bottom: 3rem;
    }

    .section-title h2 {
      font-size: 3rem;
      color: white;
      text-shadow: 2px 2px 0 #000;
    }
  </style>
</head>
<body style="background-color: #1a1a1a; color:white;">

<section class="section">
  <div class="container">
    <div class="section-title">
      <h2>Pesquisar Tabela</h2>
    </div>

    <% if (error) { %>
      <p class="has-text-danger has-text-centered"><%= error %></p>
    <% } %>

    <!-- Formulário de pesquisa -->
    <form action="/search" method="POST" style="max-width: 500px; margin: 0 auto;">
      <div class="field">
        <label style="color: #fff" class="label">Nome da Tabela</label>
        <div class="control">
          <input type="text" name="table" class="input" placeholder="Ex: dados_projeto" required>
        </div>
      </div>
      <div class="field">
        <div class="control">
          <button type="submit" class="button custom-purple is-fullwidth">Pesquisar</button>
        </div>
      </div>
    </form>

    <!-- Botão para página de clientes -->
    <div class="control" style="max-width: 500px; margin: 20px auto 0;">
      <a href="/clients" class="button custom-purple is-fullwidth">Clientes</a>
    </div>
  </div>
</section>

</body>
</html>

