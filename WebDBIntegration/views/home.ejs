<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>

  <!-- Bulma CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Fonte Montserrat -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #1a1a1a, #2b3236);
      color: white;
      font-family: 'Montserrat', sans-serif;
    }

    .section {
      padding: 4rem 2rem;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .container {
      background-color: rgba(43, 50, 54, 0.95);
      border-radius: 12px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
      padding: 3rem 2rem;
      max-width: 600px;
      width: 100%;
    }

    .section-title {
      text-align: center;
      margin-bottom: 2.5rem;
    }

    .section-title h2 {
      font-size: 2.5rem;
      color: #ffffff;
      text-shadow: 1px 1px 2px #000;
    }

    .custom {
      background-color: #a5c3d1;
      color: white;
      border: none;
      transition: background-color 0.3s ease;
    }

    .custom:hover {
      background-color: #637780;
    }

    .field label {
      color: #ddd;
    }
  </style>
</head>
<body>

<section class="section">
  <div class="container">
    <div class="section-title">
      <h2>Pesquisar Tabela</h2>
    </div>

    <% if (error) { %>
      <p class="has-text-danger has-text-centered"><%= error %></p>
    <% } %>

    <!-- Formulário de pesquisa -->
    <form action="/search" method="POST">
      <div class="field">
        <label class="label">Nome da Tabela</label>
        <div class="control">
          <input type="text" name="table" class="input" placeholder="Ex: dados_projeto" required>
        </div>
      </div>
      <div class="field">
        <div class="control">
          <button type="submit" class="button custom is-fullwidth">
            <i class="fas fa-search"></i> Pesquisar
          </button>
        </div>
      </div>
    </form>

    <!-- Botão para página de clientes -->
    <div class="control" style="margin-top: 20px;">
      <a href="/clients" class="button custom is-fullwidth">
        <i class="fas fa-users"></i> Clientes
      </a>
    </div>
  </div>
</section>

</body>
</html>
